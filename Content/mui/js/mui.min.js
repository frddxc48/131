(function (global) {
    var mui = global.mui || {};

    // Provide a minimal init hook so existing code can call mui.init() safely
    if (typeof mui.init !== 'function') {
        mui.init = function () { };
    }

    // Provide a default back handler that mirrors the behaviour used on the original site
    if (typeof mui.back !== 'function') {
        mui.back = function () {
            if (global.history.length > 1) {
                global.history.back();
            } else {
                global.location.href = '/';
            }
        };
    }

    // Expose mui back to the global scope
    global.mui = mui;
})(window);
